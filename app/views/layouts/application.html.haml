!!!
%html
  %head
    %meta{'http-equiv'=>'content-type', 'content'=>'text/html;charset=UTF-8'}
    %title= "Эксперт"
    = stylesheet_link_tag 'green'
    = stylesheet_link_tag 'screen'
    = stylesheet_link_tag 'ie'
    = javascript_include_tag 'jquery'
    = yield :head
  %body
    #wrapper.container
      #header{:class=>'span-24 prepend-top'}
        .span-3
          = link_to image_tag('logo.png'), :root, :class=>'image'
        %div{:class=>'span-15'}    
          %ul.inline_menu
            %li
              = link_to "Главная", :root
            %li
              = link_to "О проекте", "/about"
        %div{:class=>'span-6 last'}
          - if current_user
            = link_to "Личный кабинет", account_url
            \|
            = link_to "выйти", user_session_url, :method=>:delete
          - else
            = link_to "Войти", new_user_session_url
            \|
            = link_to "Регистрация", new_user_url
      %hr
      #content.span-18
        - if flash[:notice]
          #notice.success= flash[:notice]
        = yield
      #sidebar{:class=>"span-6 last"}
        Здесь интересно
      %hr
      #footer
        &copy; expert
