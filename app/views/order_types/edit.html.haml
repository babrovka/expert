%h1 Редактирование услуги

= error_messages_for :order_type

- form_for(:order_type, :url => object_url, :html => { :method => :put }) do |f|
  = render :partial => "form", :locals => {:f => f}
  %p= submit_tag "Сохранить"
